import React from 'react';

export default React.createClass({
  render() {
    return (
      <div className='container'>
        SuggestionBox || if (needs num) NumBox || Add Param || List of params as 'chips'
      </div>
    );
  }
});
